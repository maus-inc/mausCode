# Ratified decisions, project-wide

The decisions that constrain every capability, and where the full record lives. A later
change must cite this file, or the record it points at, before reversing anything here.
Nothing on this list is implied by convenience; each was chosen by the human or by an agent
under delegated authority with the reasoning written down.

## Chosen by the human

| Decision | Date | Recorded in | Effect |
| --- | --- | --- | --- |
| Display name `mausCode`, company `maus-inc`, CLI `mauscode`, data dir `.mauscode`, scheme `mauscode` | 2026-09-11 | `rebrand/decisions/open-decisions.md` D1, `rebrand/decisions/naming-system.md` | Identity constants in `src/shared/app-identity.ts`; no other product name in user-visible strings |
| Public runtime name `mausCode Runtime`, daemon `mausCode Node` | 2026-09-11 | `rebrand/decisions/open-decisions.md` D2 | JCode appears only in `UPSTREAM.md` and `NOTICE` |
| Legacy 1Code paths are read-only detection, never written, never migrated | 2026-09-11 | `rebrand/decisions/open-decisions.md` D3 | `src/shared/worktree-paths.ts`, `src/main/lib/claude-config.ts` |
| Local-only by default, control plane and update feed by build-time env, empty means off | 2026-09-11 | `rebrand/decisions/open-decisions.md` D4 | `MAIN_VITE_API_URL`, `MAIN_VITE_UPDATE_FEED_URL`; `src/main/lib/auto-updater.ts` |
| External CLIs are grouped as compatibility agents | 2026-09-11 | `rebrand/decisions/open-decisions.md` D5 | Future picker and settings copy |
| Version line restarts at `0.1.0` | 2026-09-11 | `rebrand/decisions/open-decisions.md` D6 | Decided and **not applied on `arena/01a097c4-mauscode`**, where `package.json` still reads `mauscode` `0.0.72`. Enforced by roadmap step 31 |
| Five modes, Plan, Ask, Edit, Agent, Turbo, model-agnostic | 2026-09-11 | `app/decisions/user-decisions-2026-09-11.md` | Mode taxonomy across atoms, input, routers |
| T3 adoption, contracts phase plus server evaluation spike | 2026-09-11 | `app/decisions/user-decisions-2026-09-11.md`, `app/decisions/effect-adoption-t3-layers-2026-09-11.md` | `src/shared/contracts/` verbatim plus `effect` pinned exact, isolated to that tree |
| Local-only guard is an opt-in setting, default off | 2026-09-11 | `app/decisions/user-decisions-2026-09-11.md` | `src/shared/local-only.ts` |
| JCode vendored as a copied tree with `UPSTREAM.md`, pinned SHA and patch list | 2026-09-11 | `app/decisions/provisional-assumptions.md` PA-3, ratified same day | `runtime/jcode/` |
| Lint policy: fix every finding, tests included, no exemptions, no suppressions, do not re-ask | 2026-09-12 | `app/plans/2026-09-12-jules-port-plan.md` §6.1 | Closed at 0 findings, all rules at `error` |
| No CLI product, `--parallel` escalated to sub-chat orchestration, repo inference redirected to sidebar state detection | 2026-09-12 | `app/decisions/2026-09-12-jules-feature-triage.md` rows 19-24 | Waves W3 and W6 instead of a CLI surface |
| Memory and instruction loading are a deep port of hermes-agent logic, automatic and performance-positive | 2026-09-12 | `app/decisions/2026-09-12-jules-feature-triage.md` rows 31-32 | Wave W10, steps 23 and 24 |
| MCP management follows our own UX standard, not the upstream paste-a-key panel | 2026-09-12 | `app/decisions/2026-09-12-jules-feature-triage.md` row 47 | Wave W13, step 28 |
| SDK line: Claude Agent SDK `0.3.270` plus Claude CLI `2.1.270`. Rejected: the inherited release note's `0.2.63` on the 0.2 line and the current `0.2.45` pin, because `promptSuggestions`, the 32 hook events and session control requests are 0.3 surfaces, so 0.2 leaves the parity work unimplementable | 2026-09-13 | answer to roadmap step 04, recorded in `plans/2026-09-13-mauscode-roadmap.md` §4a | Step 12 bumps the three pins together and its own 0.2 to 0.3 spike gates the land, so steps 13, 19, 20, 23, 24 and 35 unblock behind step 12 rather than behind this decision |
| Drag and drop: add `@dnd-kit`. Rejected: hand-rolled native HTML5 handlers and the standing no-new-dependency posture that backed them, which `plans/release-parity-v0.0.75-0.0.84-plan.md` §5.2 recommended and step 04 withdrew | 2026-09-13 | same | The three packages land in step 12, the only step allowed to touch `package.json` and `bun.lock`, with exact pins; steps 17, 18 and 37 use one shared `DndContext` at the agents layout root instead of native handlers, and step 30 records the bundle delta |
| Codex default: read the model catalog from the pinned CLI at runtime, with a cache, a static fallback and a loud refusal when neither source answers. Rejected: swapping the constant for one hardcoded id, which is what the auto-generated plan on #7 proposed and what leaves the two constants free to diverge again | 2026-09-13 | same | Step 05 becomes a resolver rather than a constant swap, and the live divergence between `src/main/lib/trpc/routers/codex.ts:146` and `src/renderer/features/agents/lib/acp-chat-transport.ts:41` ends there. **Shipped 2026-09-15.** The catalog is the app-server `model/list` request, whose `V2ModelListResponse__Model` carries `isDefault` and `defaultReasoningEffort`, read by `src/main/lib/providers/codex-models.ts` and cached against `codex --version`. Both literals are gone and `grep -rn "DEFAULT_CODEX_MODEL\b" src` is empty. The shared static fallback is `src/shared/codex-model-id.ts`, named for its six `<provider>-model-id.ts` siblings rather than the `codex-defaults.ts` the plan text named. Nothing in the renderer reads the resolved value yet, so the picker still shows its own static default; that is a filed follow-up, not a widening of step 05 |
| Memory: hybrid, the runtime may propose, mausCode stores, the user accepts. Rejected: the engine owning the store, which is option B of `app/research/2026-09-13-hermes-memory-spike.md` §7, and the app owning it outright, option A, which would have put the injection decision away from where the prompt is assembled | 2026-09-13 | same | Steps 24 and 43 share one proposal queue with one accept surface, and engine-side memory stays dark so nothing writes twice |
| Sign-in removal scope: gate and modal out, provider OAuth and the credential switcher kept | 2026-09-13 | ratifies `app/decisions/provisional-assumptions.md` PA-20 | Step 45 proceeds, option C refused for now, and a later override is a follow-up change with a migration note |
| Every feature item from the inherited changelog got its own question and recorded verdict | 2026-09-12 | `app/decisions/2026-09-12-jules-feature-triage.md` | 54 verdicts, waves W1-W14 |
| `ken-jo`'s built-in sign-in removal is adopted, its diff is not | 2026-09-13 | the human, reversing the Category D rejection in `ci/research/fork-network-harvest-catalog.md` | Roadmap step 45, which must still ask the scope question before deleting anything |
| `find-skills` runs before every roadmap step, and installed skills are project memory | 2026-09-13 | the human, `AGENTS.md` skill routing, `.agents/skills/find-skills/SKILL.md` fetched from `vercel-labs/skills` | Every issue body carries the rule, and a skill may never widen an approval |
| The self-improvement loop follows the agentskills.io skill format and hermes' disclosure model | 2026-09-13 | `app/research/2026-09-13-self-improvement-loop.md`, sources listed there | Steps 24, 43 and 44; `name` and `description` are validated at write time, and a skill body loads on trigger only |

| `sharp` declared as a devDependency so `scripts/generate-icon.mjs` runs, the only other exception to the no-new-dependency posture | 2026-09-14 | answer to the decision batch, recorded here and enforced by step 12 | The script imports `sharp` at `scripts/generate-icon.mjs:21` while `package.json` declares none, so it works only by accident of hoisting; step 12 adds the exact pin, and step 31 verifies the script runs instead of retiring it |
| Package identity settled at `dev.mausinc.mauscode`, display name `mausCode`, npm name and CLI `mauscode`, plus a CI branding guard | 2026-09-14 | same, closing `questions.md` items 5 and 6 | `AGENTS.md` facts state the rule, `app/decisions/provisional-assumptions.md` PA-1 is corrected to the tree value, and step 31 adds a check that fails on `21st` or T3 identity strings outside vendored comments and licence files |
| Data-egress doctrine: nothing leaves the machine that the user did not ask for, and anything that can leave is visible where the user can see it | 2026-09-14 | same, closing `questions.md` item 17 | The test every network path must pass, written into `AGENTS.md` §Values and rules and step 27's acceptance criteria; no Honcho-style persistent user model, and no user-derived profile stored anywhere, local included |
| Vendored contracts stay: keep, absorb per use, and verify with a written ledger | 2026-09-14 | `app/plans/contracts-adoption.md`, corrected the same day | Deletion refused. Measured again on 2026-09-14 at `f5506b9`, `src/shared/contracts/` holds 44 source files at 19,395 lines plus 23 test files at 5,465 lines, 24,860 total, reproduced two ways by `wc -l src/shared/contracts/*.ts \| tail -1` and by a per-file sum of `grep -c ''`, and no file lacks a trailing newline. The ledger's first pass recorded 19,439, 5,488 and 24,927, each one line per counted file too high, so its claim to correct the plan corpus's 24,860 was itself the error; the ledger now carries the corrected rows and a note saying what changed. Those 23 test files run under `npm run test`, so the tree is a live validation corpus rather than dead weight |
| Release channels are defined now, with a notary placeholder and no signing until a human provisions certificates | 2026-09-14 | same, answering `questions.md` item 7. **Superseded the same day** by batch 4 below: the placeholder was deleted, not left to fail | Step 32 grows from an unsigned build into alpha and stable channel definitions; the notarization half of this answer is void |
| Handoff documents are ephemeral; the convention is stated in `AGENTS.md` and nothing else | 2026-09-14 | same, answering `questions.md` item 16 | No root `HANDOFF.md` or `STATUS.md` is committed, durable notes go in `.dump`, and `.dump/ci/research/HANDOFF-fork-harvest-context.md` is marked superseded rather than edited, because a dated record stays truthful |

| Demo GIFs stay where they are, unreferenced, until a real README pass | 2026-09-14 | answer to the batch, closing `questions.md` item 9 | `assets/worktree.gif`, `plan-mode.gif` and `cursor-ui.gif`, 10.5 MB total with the old name burned into the frames, are not deleted and not moved; step 31 stops treating them as a loose end, and re-recording is a docs task for the interface that will exist by then |
| Stray root artifacts move to `assets/branding/`, kept in git, marked design inputs | 2026-09-14 | same, closing item 13 | The three font files and the `new mauscode branding/` masters, about 3.9 MB, move into `assets/branding/` with a line stating they are design inputs and never build inputs; `package-lock.json` is already gone, and it stays gone, because it is what broke the renderer build once |
| `tsgo` becomes a second CI gate after its disagreements with `tsc` are resolved | 2026-09-14 | same, closing item 11 | Step 02 owns it. `tsc` stays the blocking zero-error gate, `tsgo --noEmit` joins CI once the delta is measured and each remaining disagreement is justified or fixed; neither tool is suppressed to make the other green |
| `.github/workflows/lock-regen-temp.yml` stays as the documented escape hatch | 2026-09-14 | same, closing item 14 | It is renamed to a supported, manually dispatched workflow with a comment saying when to use it, because lockfile regeneration has broken this branch twice; step 31 writes the documentation rather than deleting the file |

| No signing and no notarization anywhere, unsigned artifacts with checksums are the product | 2026-09-14 | answer to the final batch, closing `questions.md` item 8 | Step 32 drops the notary placeholder entirely and documents the one-time Gatekeeper bypass for users. Checksums are the integrity story. Auto-update stays off unless `MAIN_VITE_UPDATE_FEED_URL` is set, which is what makes an unsigned artifact safe to publish |
| All four release targets stay in the release matrix, CI minutes accepted | 2026-09-14 | same | mac x64, mac arm64, windows x64, linux x64, each doing full install, the platform binary downloads, the build and packaging. Cross-platform breakage is caught at release time rather than nightly, and the cost is four times the minutes on the human's account |
| 1Code data stays read-only detection permanently, no import button, no migration | 2026-09-14 | same, closing `questions.md` item 15 | We find old data and say what was found. There is no copy-only import and no dry run, so nothing a user has can be written by us, unless a real demand arrives with its own design |
| Issue bodies lag the files and that is accepted; the files are the source of truth | 2026-09-14 | same, recorded in `plans/2026-09-13-mauscode-roadmap.md` §4b | The integration can create issues but not edit one, comment on one or label one, all verified 403 this session, so no re-sync work is scheduled. Every agent reads `.dump/app/roadmap/NN-<slug>.md`, and `{{SNN}}` resolves to issue `#NN+2` |
| Planning is the deliverable of this session; no roadmap step is executed from it | 2026-09-14 | same, recorded as §4c in the roadmap plan | Nothing further lands on `arena/01a097c4-mauscode` from this session. Steps 01 to 03 are ready to be taken by whoever the human assigns, and each carries its own verification and benchmark obligation |

## Chosen by an agent under delegated authority

| Decision | Where | Reason in one line |
| --- | --- | --- |
| Port upstream verbatim with attribution, or do not port it | `docs/backend-porting-recipe.md` §0 | A silent rewrite loses the ability to re-port and to credit |
| Capability profile decides what the UI offers; a backend that cannot report a capability must not have it | `app/decisions/provider-agnostic-backends-upstream-policy-2026-09-11.md` | No silent special powers |
| Credentials are references, stored only via `src/main/auth-store.ts` and `safeStorage` | Same file, plus `AGENTS.md` | One owner, one encryption path |
| Held credential instead of writing a provider's own config file, for qwen, cline and openclaw | `app/decisions/{qwen,cline,openclaw}-provider-adoption-2026-09-11.md` | Those CLIs store keys in plaintext; the user's files stay untouched |
| Ratchet the inherited debt, never fake green | `ci/plans/initial-ci-plan.md`, `ci/decisions/2026-09-11-lint-gate-and-format-sweep.md` | A gate that fails on debt nobody created trains people to ignore CI |
| Vendored contracts are vocabulary until used, adopt per use | `app/decisions/effect-adoption-t3-layers-2026-09-11.md` and step 13 of the roadmap | 24,860 lines with zero importers is not a live path |
| OpenSpec changes are scaffolded by hand, validated where the CLI exists | `app/decisions/2026-09-11-openspec-cli.md` | The CLI is unobtainable from verified sources |

## Deliberately rejected

| Rejected | Why |
| --- | --- |
| Blanket replacement of every `21st` or `1code` string | Breaks legacy worktree detection, attribution comments and a file-format enum |
| Auto-migrating 1Code data on first launch | Moving live worktrees with uncommitted changes is a data-integrity decision needing its own design |
| `npm overrides` to fix the renderer build | The failure was dependency drift in a sandbox, not a code bug |
| Four-host pull request provider registry | We are GitHub-only through `gh`; four providers is four maintenance surfaces |
| Wholesale merges from any fork, and every fork's deletions | Their removals are exactly what the human asked to keep away |
| A pirate persona day feature | Asked and declined |
| Wholesale replacement of the credential and OAuth plumbing on the strength of the auth-removal decision | Provider credentials and MCP OAuth are not the app account; step 45 keeps `auth-store.ts`, `auth-manager.ts` and the loopback callback server that MCP auth depends on |
| A persistent psychological model of the user, which upstream implements with Honcho | Needs its own consent conversation, held at `questions.md` item 17 |
