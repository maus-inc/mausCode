## 0. Meta

| Field | Value |
| --- | --- |
| Step | 12 of 45, parity P1-1, P1-2, P1-3 and P2 |
| Area | deps, main, renderer |
| Risk | high |
| Depends on | {{S04}} decision 1, {{S11}} |
| Blocks | {{S13}}, {{S19}}, {{S20}}, {{S23}}, {{S35}} |
| Estimate | medium |

## 1. Outcome

The Claude Agent SDK moves to `0.3.270`, the bundled CLI to `2.1.270` and the Codex pin follows, as ratified on 2026-09-13, the three missing tool registry entries appear, the adaptive thinking, effort and prompt-suggestion surfaces become reachable, and `@dnd-kit` arrives here with exact pins, because this is the only step allowed to touch `package.json` and `bun.lock` and the approved drag and drop decision needs it.

## 2. Why it matters

Three pins move as one because they are the same product's wire format: SDK at `0.2.45` and `claude:download` at `2.1.45` in `package.json`, verified this session, with Codex at `0.137.0`. The parity plan records that `MultiEdit` is absent from the tool registry so a batch edit renders as nothing, `tool-Agent` is absent so a sub-agent's own work is invisible, and `TaskOutput` is absent so background Bash output has no home. Those are three small entries, and they unlock the fan-out and critic steps.

## 3. Evidence

| Fact | Path | Level |
| --- | --- | --- |
| Current pins | `package.json` dependencies, scripts `claude:download` and `codex:download` | E1, this session |
| Tool registry is one file with one entry per tool name | `src/renderer/features/agents/ui/agent-tool-registry.tsx` | E1, read before editing |
| Download scripts verify sha256 against a manifest and keep a marker file | `scripts/download-claude-binary.mjs`, `scripts/download-codex-binary.mjs` | recorded in `FULL-REVIEW.md` §6.5 |
| The typed stream work this bump enables | step 09, and `src/main/lib/claude/transform.ts` | by contract |
| Hooks: the plan counts 32 hook events in the 0.3 line | `.dump/app/plans/release-parity-v0.0.75-0.0.84-plan.md` P2 | recorded, verify against the changelog you fetch |

## 4. Read first, and what already exists

`docs/backend-porting-recipe.md` §4 and §7, because a dependency bump that changes what a backend can do is a capability change. `src/shared/local-only.ts` and `src/main/lib/permissions` from step 10 decide what new surfaces may be enabled by default. Do not write a new adapter; `src/main/lib/providers/claude.ts` already reports capability strings.

## 6. Implementation plan

1. Read the upstream changelog for every version between the current pin and the target, and put the list of behaviour changes in the PR. This is the only step allowed to touch `package.json` dependencies, `bun.lock` and the download script pins, and it must say so.
2. Bump the three pins in one commit, regenerate `bun.lock` with `bun install`, and confirm `bun install --frozen-lockfile` passes on the result.
3. Add the three registry entries with the renderer shapes the SDK actually emits, each with a test that renders a captured payload, plus a fixture per tool.
4. Wire adaptive thinking, effort and prompt suggestions to the mode and model selectors, keeping the mode taxonomy ratified in `AGENTS.md` untouched. No new mode.
5. Re-run the recipe §4 closed-dialect check on the new chunk kinds in the same commit, and update `src/main/lib/claude/transform.ts` types from step 09 if the stream shape changed.
5b. Add `@dnd-kit/core`, `@dnd-kit/sortable` and `@dnd-kit/utilities` in their own commit, exact pins, `bun install` to regenerate `bun.lock`, and a note that steps 17, 18 and 37 consume them through one shared context at the agents layout root. Record bundle bytes per target with and without them, since this is the dependency a performance claim will be checked against.
5c. Declare `sharp` as a devDependency with an exact pin. `scripts/generate-icon.mjs:21` imports it while `package.json` declares nothing, so the icon script runs only when a transitive copy happens to be hoisted; a fresh install with `--ignore-scripts` cannot run it. This is the second and last approved dependency exception after `@dnd-kit`, and the reason it belongs here is that this is the only step allowed to touch `package.json` and `bun.lock`.
6. Confirm the binary integrity path still verifies: run `bun run claude:download` and `bun run codex:download` and record the hash check outcome.
7. `bun run build` with the 4 GB heap flag and `bun run package:linux` after the bump, because a native module mismatch only shows up at packaging.

## 8. Boundaries

- Always: one dependency step, exact pins, `bun.lock` regenerated by the command CI runs.
- Ask first: a new transitive dependency, any change to a permission or egress default the bump introduces, and any change to what is sent to the provider.
- Never: `bypassPermissions` reaching the new options surface, an `npm overrides` block to dodge a conflict, a caret on `effect` or on any pin CI resolves, or a version bump smuggled into a feature PR.

## 10. Acceptance criteria

- [ ] `package.json` shows the decided SDK, CLI and Codex versions, and `bun install --frozen-lockfile` is clean.
- [ ] `MultiEdit`, `tool-Agent` and `TaskOutput` render in a captured transcript, with tests.
- [ ] Every behaviour change read in step 1 is either handled or recorded as a known gap in `.dump`.
- [ ] Gates green, `bun run build` green, `package:linux` green.
- [ ] No new dependency beyond the version moves and the three `@dnd-kit` packages the human approved, each pinned exactly, with the lockfile proof and the bundle delta recorded.

## 11. Verification

```sh
bun install --frozen-lockfile
bun x biome check . && npm run typecheck && npm run test && npm run test:node
NODE_OPTIONS=--max-old-space-size=4096 bun run build
bun run claude:download && bun run codex:download && bun run package:linux
```

## 12. Benchmark record

Bundle size per target and turn latency for one scripted prompt, before and after, in `.dump/app/benchmarks/`.

## 13. Rollback

Revert the pins and `bun.lock` together, and re-run install so the tree matches. A partial revert is worse than either version.

## 14. Out of scope

The Codex schema drift and parity verification that this bump makes necessary, which is {{S35}}. Model tiers were rejected in triage row 36.

## 15. Handoff notes

Write the upstream behaviour list to `.dump/app/research/2026-09-13-sdk-0-3-bump.md` and name it in {{S19}} and {{S20}}, which assume those three tools exist.
